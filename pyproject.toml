[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "carrot-rl"
version = "0.1.0"
description = "A lightweight, backend-neutral runtime for distributed robot learning."
readme = "README.md"
requires-python = ">=3.12"
license = { file = "LICENSE" }
dependencies = [
  "ray>=2.47",
]

[project.optional-dependencies]
sft = [
  "lerobot[dataset]==0.6.1",
  "diffusers==0.35.1",
  "pyyaml>=6",
  "torch>=2.6",
  "transformers==4.51.3",
  "wandb>=0.16",
]
dev = [
  "pytest>=8",
  "pytest-timeout>=2.3",
  "ruff>=0.9",
]

[project.scripts]
carrot-train-sft = "carrot.entrypoints.train_sft:main"
carrot-compute-pi05-stats = "carrot.entrypoints.compute_pi05_stats:main"

[tool.hatch.build.targets.wheel]
packages = ["src/carrot"]

[tool.pytest.ini_options]
addopts = "-ra"
pythonpath = ["src"]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
