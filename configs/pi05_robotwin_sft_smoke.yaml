model:
  path: /mnt/ceph-hz1-csp/mm-base-plt2/nrwu/hf-hub/Miical/pi05-base
  tokenizer_path: /mnt/ceph-hz1-csp/mm-base-plt2/nrwu/hf-hub/Miical/pi05-base
dataset:
  repo_id: lerobot/robotwin_unified
  root: /mnt/ceph-hz1-csp/mm-base-plt2/nrwu/hf-hub/lerobot/robotwin_unified
  num_workers: 0
  norm_stats_path: null
  adapt_aloha: true
  delta_actions: true
  image_keys:
    - observation.images.cam_high
    - observation.images.cam_left_wrist
    - observation.images.cam_right_wrist
optimizer:
  learning_rate: 2.5e-5
  weight_decay: 1.0e-10
  betas: [0.9, 0.95]
  eps: 1.0e-8
  warmup_steps: 0
  decay_steps: 1
  decay_learning_rate: 2.5e-6
  max_grad_norm: 1.0
fsdp:
  enabled: true
  param_dtype: bfloat16
  reduce_dtype: float32
  reshard_after_forward: true
steps: 1
micro_batch_size: 1
global_batch_size: 1
log_freq: 1
save_freq: 1
output_dir: /mnt/ceph-hz1-csp/mm-base-plt2/nrwu/work/carrot/outputs/pi05_robotwin_smoke
num_gpus: 1
num_nodes: 1
